# shared/auth

Admin session and API guards. Legacy: lib/admin-api.ts, admin-authorization.ts, auth-server.ts.

**Phase status:** Scaffold only — do not import from here until Phase 1 shims (later phase).

