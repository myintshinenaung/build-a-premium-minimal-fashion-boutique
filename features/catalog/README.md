# Catalog feature

**Status:** Scaffold (Phase 0–1)  
**Migration phase:** 5

Products, categories, variants, listing, and PDP.

## Legacy locations (unchanged)

lib/storefront/catalog.ts, map-catalog.ts, variants.ts; lib/repositories/product-*; components/product/

## Target ownership

All catalog logic and UI move under `features/catalog/` per layer folders.

