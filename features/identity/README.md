# Identity feature

**Status:** Scaffold (Phase 0–1)  
**Migration phase:** 8

Admin authentication and authorization.

## Legacy locations (unchanged)

lib/admin-auth*.ts; lib/supabase/auth-*; proxy.ts; components/admin/auth/

## Target ownership

All identity logic and UI move under `features/identity/` per layer folders.

