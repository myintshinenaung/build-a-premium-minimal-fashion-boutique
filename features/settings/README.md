# Settings feature

**Status:** Scaffold (Phase 0–1)  
**Migration phase:** 3

Store brand, contact, hero, and SEO metadata.

## Legacy locations (unchanged)

lib/storefront/settings.ts, map-settings.ts, metadata.ts; SettingsForm

## Target ownership

All settings logic and UI move under `features/settings/` per layer folders.

